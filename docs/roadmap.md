# ロードマップと進行状況

Issue はタスクの一覧 (何をやるか) を管理し、このファイルはフェーズの進行 (今どこにいて、次に何を閉じるか)
を管理する。役割を分けているので、Issue の本文や Project の Status をここへ写さない。
個々の Issue の状態は Issue の Open/Closed が正本で、ここに書くのは **フェーズ単位の状態と、
フェーズを閉じるために残っている Issue 番号** だけ。GitHub の milestone はこのフェーズの写しで、
Issue 一覧をフェーズで絞り込むためだけに使う (`scripts/wf-milestone.sh`)。

フェーズの完了は「ゴールを実際に試せた」ことで判定する。Issue が全部閉じても、試して動かなければ
閉じない。逆に、Issue が残っていてもゴールが試せていれば残りは次フェーズ以降へ送る。

更新のタイミング: フェーズを閉じた時、フェーズの Issue 集合を変えた時、着手中の Issue を変えた時。
セッションの最終報告 (handoff) には書かず、ここに書く。

## フェーズ

| # | フェーズ | ゴール (試せること) | 状態 |
| --- | --- | --- | --- |
| P1 | 最低限を試せる | worktree ごとのタブを開き、tmux セッションに attach し、タブに代表状態が出る | **完了** (2026-09-07) |
| P2 | 基本機能を試せる | 日常の開発を1日このアプリで回せる | **進行中** |
| P3 | バグ改修 | P1〜P2 の dogfooding で見つかった不具合が直っている | 未着手 |
| P4 | モバイル版作成 | iPhone から SSH で同じ tmux に attach できる | 未着手 |
| P5 | モバイル版チェック | 実機 + VPN で同時 attach・復帰・通知が動く | 未着手 |

P1〜P3 を通してから P4 に入る。P3 は「P2 まで使ってみた結果」が入力なので、P2 完了前には着手しない。

## P1 最低限を試せる (完了)

2026-09-07 に完了。判定はゴールを実際に試した結果による (Issue が閉じたことではない)。
main のビルドからアプリを起動し、次の4点を実測で確認した。

- worktree ごとのタブが2つ以上並ぶ
- タブ切替でその worktree 専用の tmux セッションが生成・attach される。セッション名は §3.5 の
  規則から独立に算出した期待値と一致した
- タブに代表状態が出る。非アクティブ window に置いた agent プロセスを検出し、状態を確定できない
  ケースを `Unknown` として独立表示した (`Working` / `Idle` へ丸めていない、§12.3)
- active window を 0→2→1→2→0 と切り替えた16サンプルでタブバーの画像ハッシュが完全一致し、
  代表状態が window 切替の影響を受けない

閉じた Issue: #153 (pane 集合の束ね層) / #154 (アプリ配線の縦スライス) / #25 (タブ UI + 代表状態) /
#137・#138 (設計判断)。

## P2 基本機能を試せる (進行中)

- Viewer Drawer の中身: #29 / #30 (+#33 G5 snapshot はここに畳む) / #31
- #136 (Active 状態の永続化)、#92 (Working 検出の遅延)
- 着手中: なし

閉じた Issue: #171 (Project Root の永続 tmux セッション) / #160 (到達不能 worktree の保持) /
#28 (Viewer Drawer の枠組み)。

## P3 バグ改修

dogfooding で症状が出たものを直す。レビュー由来の堅牢性・衛生課題 (Adapters / App・libghostty
移植残 / Tests / scripts・CI) は milestone `P3 バグ改修` を parking lot として置き、**症状が出た時だけ**
P1・P2 へ引き上げる。症状が出ないまま P3 に入ったものは、P3 の中で優先度を付け直す。#27 (長時間稼働の
手動検証) もここ。

## P4 モバイル版作成

- 前提の設計判断: モバイル renderer (G2, #34)。P3 完了までは決めない
- v1 は SSH + terminal のみ (SwiftNIO SSH、tmux attach)。構造化データ (hostctl over SSH、G4 #32) は v2 へ分離
- コードを書く前に §20.3 の初期段階 (外部 SSH アプリ + tmux attach) を実機で体験し、専用アプリに要るものを決める

## P5 モバイルチェック

手動検証のみ。実機・Tailscale・複数デバイス同時 attach・アプリ復帰・ローカル通知。Push relay の要否はここで判断する。

## 閉じたフェーズ

旧 milestone (M0 運用整備 / Phase 2a / 2b / 3 / 4 / 後続ゲート) は 2026-09-06 に上の5フェーズへ
切り直した。閉じた Issue は旧 milestone に残したままで、履歴として読める。
