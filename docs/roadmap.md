# ロードマップと進行状況

Issue はタスクの一覧 (何をやるか) を管理し、このファイルはフェーズの進行 (今どこにいて、次に何を閉じるか)
を管理する。役割を分けているので、Issue の本文や Project の Status をここへ写さない。
個々の Issue の状態は Issue の Open/Closed が正本で、ここに書くのは **フェーズ単位の状態と、
フェーズを閉じるために残っている Issue 番号** だけ。GitHub の milestone はこのフェーズの写しで、
Issue 一覧をフェーズで絞り込むためだけに使う (`scripts/wf-milestone.sh`)。

フェーズの完了は「ゴールを実際に試せた」ことで判定する。Issue が全部閉じても、試して動かなければ
閉じない。逆に、Issue が残っていてもゴールが試せていれば残りは次フェーズ以降へ送る。

更新のタイミング: フェーズを閉じた時、フェーズの Issue 集合を変えた時、着手中の Issue を変えた時。
セッションの最終報告 (handoff) には書かず、ここに書く。

## フェーズ

| # | フェーズ | ゴール (試せること) | 状態 |
| --- | --- | --- | --- |
| P1 | 最低限を試せる | worktree ごとのタブを開き、tmux セッションに attach し、タブに代表状態が出る | **完了** (2026-09-07) |
| P2 | 基本機能を試せる | 日常の開発を1日このアプリで回せる | **進行中** |
| P3 | セッション把握と判断待ち | 全Agent paneの目的・現在地と判断待ちを一覧で把握し、対象へ戻れる | 未着手 |
| P4 | モバイル通知 | iPhoneロック中にも判断待ち・タスク完了を通知する | 未着手 |
| P5 | モバイル操作 | 通知から回答・プロンプト送信・新規タスク開始まで行える | 未着手 |

P1〜P3 を通してから P4 に入る。P3 は「P2 まで使ってみた結果」が入力なので、P2 完了前には着手しない。

## P1 最低限を試せる (完了)

2026-09-07 に完了。判定はゴールを実際に試した結果による (Issue が閉じたことではない)。
main のビルドからアプリを起動し、次の4点を実測で確認した。

- worktree ごとのタブが2つ以上並ぶ
- タブ切替でその worktree 専用の tmux セッションが生成・attach される。セッション名は §3.5 の
  規則から独立に算出した期待値と一致した
- タブに代表状態が出る。非アクティブ window に置いた agent プロセスを検出し、状態を確定できない
  ケースを `Unknown` として独立表示した (`Working` / `Idle` へ丸めていない、§12.3)
- active window を 0→2→1→2→0 と切り替えた16サンプルでタブバーの画像ハッシュが完全一致し、
  代表状態が window 切替の影響を受けない

閉じた Issue: #153 (pane 集合の束ね層) / #154 (アプリ配線の縦スライス) / #25 (タブ UI + 代表状態) /
#137・#138 (設計判断)。

## P2 基本機能を試せる (完了)

当初の Issue はすべて閉じた (#33 は前半を検証して閉じ、残りを #229 へ送る) が、2026-09-08 の
P2 全体レビュー (下記) で「1日回す」を阻む欠陥が見つかり、Issue 集合を組み直した。

- **2026-09-10 に完了。** #316 の個別項目をすべて実機で確認して PASS した (#234 の overlay と入力誤送信、
  #235 の history-limit / window-size / `#` を含むパスの cwd、#236 の設定反映、#237 の Inactive、
  #238 の ⌘N 不在と ⌘W、#239 の CPU)。結果は #316 のコメントにある
- **「日常の開発を1日このアプリで回せた」の実地確認は、ユーザーの判断でひとまず完了として扱った** —
  1日を通しての利用を待たずにフェーズを締めている。以後の利用で支障が出たものは、その時点で証拠とともに
  Issue を起票し、フェーズを跨いで扱う
- 実装 Issue は 2026-09-09 に全件閉じた — #90 (fixture の版数) / #235 (session 生成の配線) /
  #234 (detach 後の復帰) / #237 (Inactive タブ非表示) / #239 (観測コスト) / #236 (端末設定) /
  #238 (二重 window)。隣の tmux pane 2本を並列レーンとして回し、7件を1日で通した
- 実機 E2E は #316 へ集約した。実アプリの window をアクティブ化する確認はユーザーの PC 操作と競合するため、
  各 Issue では隔離ソケットで完結する計測までを行い、スクリーンショットと実 HID 操作を伴う確認は #316 で
  まとめて行った

閉じた Issue: #73 (統合テストを CI で走らせる) / #286・#290 (tmux 3.7c の外界ドリフト。#73 の CI が
初回実行で検出した) / #243 (スキャン失敗で Active 指定が消える) /
#171 (Project Root の永続 tmux セッション) / #160 (到達不能 worktree の保持) /
#28 (Viewer Drawer の枠組み) / #177 (Viewer Drawer の表示方法の寿命) /
#29 (ファイルブラウザ + コードビューア) / #31 (ripgrep 検索) / #136 (Active/Inactive の永続化) /
#202 (Active/Inactive の操作とアプリ配線) / #233 (タブ切替で first responder が移らない) /
#217 (入力欄の dim で Idle/Completed を判定) /
#240・#246 (コメント送信の状態と同一性の確認) / #278 (端末とコメント欄のフォーカス調停) /
#241 (diff が gitconfig 依存で別リポジトリのファイルへコメントが送られる) / #92 (ターン開始直後の Working 検出) /
#242 (競合中の worktree で Diff にファイルが出ない) /
#203 (待機中の画面書き換えで Idle にならない) / #30 (Diff ビューアと snapshot) /
#208 (Diff レビューコメントと実装 Agent pane への送信) / #220 (時間と CPU を測るテストの分離) /
#215・#137・#138 ほかの設計判断。

### このフェーズで切り出したもの

大きすぎる Issue は、1 worktree に収まる単位へ割った。フェーズが未実装機能待ちで伸びるのを防ぐため。

- #29 → #183 (§7.3 のファイル別 Git 履歴・過去 commit 時点のコード・blame)。フェーズ未割当
- #31 → #195 (§8.1 の検索結果を fresh Agent へまとめて渡す)。渡す先の決定が §10 と §9.2 に
  属し、検索そのものと独立しているため。フェーズ未割当
- #30 → #208 (§9.2 のコメントと送信)。§9 全体では1 worktree に大きすぎたため。P2 内で完了
- #33 → #229 (Gate 5 の残り3項目)。消失後の復元 fallback・device 別 Drawer 状態・worktree 削除後の
  Consultation／Evidence 参照は、いずれも未実装機能に依存して現時点では検証できない。フェーズ未割当。
  #33 に残した前半2項目 (snapshot の固定と Refresh) は実機で検証して閉じた
- CI の都合で #220 (時間と CPU を測るテストを専用 target へ分離)。既存のタイミング依存テストが
  並列実行の負荷で落ち、無関係な PR を止めていたため

### このフェーズで確定させた設計判断

着手前に決めないと実装が設計書を追い越すため、まとめて確定させた (PR #201 / #223)。

- Git/Diff (§9.1.1〜§9.1.3 / §9.2): base branch の決定方法、Diff range は merge-base 起点、
  未 commit 変更は4区分すべて含めて区別表示、comment anchor は出所を含む6要素
- worktree 復元 (§3.2.1 / §22.1): 停止中に現れた worktree は Inactive、停止中に消えた worktree は
  到達不能として保持、GRDB 採用までは Application Support 配下の JSON へ暫定保存
- メイン pane の登録方法 (§12.7 / §25): 実装 Agent pane はユーザーが明示的に選び、候補が1つでも
  自動決定しない

§9.2.1 のテキスト注入の警告 UI だけは未確定のまま §25 に残っている。Diff の中核仕様と独立で、
#208 の着手を妨げなかったため。

### App/ の UI 配線の検証手段を持った

#230 で「main では行選択が動かない」という報告が出たが、計測の結果**製品コードは壊れておらず、
エージェント側の UI 検証手順の誤り**だった (not a bug で close)。非アクティブなウィンドウへの初回
クリックはウィンドウのアクティブ化に消費されて `onTapGesture` に届かない一方、SwiftUI の Button は
同じ状況でも反応するため、「クリックは届いているのに行だけ無反応 = 実装の欠陥」と誤って結論できてしまう。

同じ罠を次のエージェントが踏み直さないよう、実機検証のハーネスを `scripts/` へ入れた (#231)。
最終バンドルを起動し、Accessibility で要素位置を取得して実 HID クリックを送り、ラベルを検証する。
上の罠はハーネス側で閉じてあり、「なぜ」はスクリプトに記録した。CLAUDE.md の
「App/ の UI 配線は動かして確認する」もこのハーネスを指すよう更新した。

この一件は、UI 層に GREEN が効かないことの実例でもある。710 tests が通り敵対的レビューで
Critical 0 の状態でも、行選択が壊れていれば §9.2 の導線は1つも成立しない。実際 #208 の手動確認では
`.textSelection(.enabled)` と行選択の競合という実在の欠陥が見つかっている。

### レビュー由来の派生 Issue

12件を `P3 バグ改修` パーキングロットへ送った (#197〜#200 / #211〜#214 / #216・#217 / #222 /
#224〜#228)。現行フェーズの受け入れ条件を塞がないものは現フェーズに数えない。日常利用での
再現を待たずに評価するのはデータ損失と誤入力経路に限る — #208 のレビューが見つけた
「Agent へ送る文面に別ファイルのコードが載る」欠陥は後者に当たるため、パーキングせず差し戻した。同じ
理由で #246 (tmux server 再起動で振り直された pane ID の取り違え) をパーキングロットから P2 へ引き上げた。

### P2 全体レビュー (2026-09-08)

Issue が全部閉じた時点で、実地確認の前に main (6bc2186) を 6 領域 (tmux 境界 / Agent 状態モデル /
Git 境界と worktree 寿命 / Diff とコメント送信 / App 層 / ファイル閲覧と検索) に分けて敵対的レビューにかけた。
外部 CLI の主張はすべて隔離環境で計測。結果は Critical 4 / Major 17 / Minor 多数で、**現状の main では
ゴールが成立しない**と判断した。理由は3つ。

- 中核機能が現行版で動かない: Claude Code 2.1.263 は空の入力欄にサジェストを常時描くため、Ready for
  Review / Idle が一度も出ない (#217)。パーキング時の「起動直後だけ」という前提が計測で否定された
- 端末の基本動作に穴がある: タブ切替で first responder が移らず打鍵が別 worktree の Agent に届く (#233)、
  detach したタブが復帰できない (#234)、端末設定を変える手段が無い (#236)
- 設計書どおりに作った層が App から呼ばれていない: session 生成・Close・pane 操作・tmux 版数検出は
  ライブラリにあるだけで、§4.4 の history-limit も §4.2 の window-size も本番で適用されていない (#235)

上の「残る Issue」11件 (#233〜#243) を P2 に追加し、パーキングロットから #217 / #73 / #90 を引き上げた。
Close 経路の Critical (#244、detached HEAD が無警告で消える) は Close が App 未配線のため P2 に入れず、
配線前に §3.4 を確定する条件として `設計判断` を付けてパーキングロットに置いた。残り 15 件
(#245〜#259) は `P3 バグ改修` へ。

教訓として残すもの: (1) Issue が全部閉じたことと機能が使えることは一致しない — #24 / #13 は閉じているが
Close と pane 操作はユーザーから使えない。(2) 統合テストは CI で一度も走っておらず (#73)、fixture に版数が
無いため (#90) 外界のドリフトを 710 件の GREEN が検知できなかった。(3) パーキング時の影響評価が計測に
基づいていないと、#217 のように前提が誤ったまま駐車される。

### 追加レビュー後の進め方 (2026-09-10)

TODO の正本は GitHub Issues / Project とし、このファイルには実装チェックリストを複製しない。
レビュー由来の保留課題は、P2 の受け入れ条件を阻むと計測で判断できたものだけを現行フェーズへ移す方針を
とった。実際に移したものは無く、#276 (入力の安全性) / #347 (読取上限) / #319 (検証の安定性) は
パーキングロットのまま順に処理している。

- Close を App に接続する作業は #244 の実装完了を前提とする。detached HEAD の Close 拒否は
  2026-09-08 に決定済み (PR #261、設計書 §3.4)。全体レビュー時点の「設計判断待ち」は解消しており、
  再決定ではなく確定した仕様の実装が必要となる
- P4 は #190 → #191 の通知経路を検証する。P5 は #34 / #32 → #192。後続の閲覧機能
  (#183 / #193 / #195 / #229) は、依存する機能の準備ができた段階で割り当て、
  中核フェーズの完了条件へ一括追加しない

## P3 セッション把握と判断待ち (進行中)

- 対象: #188 → #189 / #190、長時間稼働の検証は既存 #27
- 着手中: パーキングロットの #319 (FileChangeWatcher の flaky) / #244 (detached HEAD の Close 拒否)。
  #244 は Close を App へ配線する前提なので、P3 の中核より先に片づける
- 3プロジェクト各2〜3タスク、1タスク内の複数Agent paneで利用する。全paneの目的・現在地と
  状態アイコンを独立Overviewに表示し、タスク切替なしで閲覧し、対象paneへ直接戻れることを試す。
- paneの応答終了をタスク完了と誤認せず、ハーネスの明示信号に基づくタスク完了通知と、
  どのpaneからの判断待ち通知も確認する。連携なしでも手入力の概要と通常Terminalを利用できる。
- 既存不具合はこの利用の妨げになるものを優先する。データ損失・誤送信等は日常利用での再現待ちにせず評価する。

旧milestone `P3 バグ改修` はレビュー由来の保留課題の置き場として残すが、現在のP3ではない。
保留課題の全消化はフェーズ完了条件にしない。影響を評価して必要なIssueだけ現行フェーズへ移す。

## P4 モバイル通知

- 対象: #191 (P3の #190 に依存)
- Macアプリ起動中、外出先のiPhoneでアプリを開かずロックした状態でも、判断待ちとタスク完了の通知が届く。
- opt-inの通知中継を有効にして実機検証する。配信失敗・重複・通信復帰の結果も記録する。
- Terminal rendererやhostctl全機能の完成を前提にせず、通知経路を先に通す。

## P5 モバイル操作

- 対象: #34 / #32 → #192。既存のGate 2/Gate 4はこのフェーズへ移す。
- 外部SSHアプリとtmux attachで先に実機体験し、専用アプリに必要な操作を決める。
- 通知から該当タスク/paneへ戻り、回答・プロンプト送信・新規タスク開始を行える。
- 実機・VPN・複数デバイス同時attach・アプリ復帰・消失した対象を検証する。構造化データは
  この導線に必要な範囲から導入し、Diff/Evidence全機能を完了条件に含めない。

## 後続の閲覧機能

Diffは比較元を指定して別窓で開く要件を設計書§1.2へ記録した。P2のDrawer実装は維持する。
別窓の組み込み／外部ツール連携の選択は #193 (フェーズ未割当) で扱う。Gitツリー表示は任意候補とし、
通知までの中核体験を遅らせない。

## 閉じたフェーズ

旧 milestone (M0 運用整備 / Phase 2a / 2b / 3 / 4 / 後続ゲート) は 2026-09-06 に上の5フェーズへ
切り直した。閉じた Issue は旧 milestone に残したままで、履歴として読める。

2026-09-07の目的再確認によりP3〜P5を再編した。P1・P2のIssue集合・本文・着手中タスクは変更していない。
旧 `P4 モバイル版作成` / `P5 モバイル版チェック` は対象Issueの移動後に閉じ、旧P3は上記の保留用途で残す。
